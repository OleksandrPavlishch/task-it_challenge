// 'use strict';
// (function () {
	// var contactsBlock = document.getElementById('contacts')
	// 	, contactsParallaxBackground = document.getElementById('contacts_parallax_background');

	// contactsBlock.onmousemove = function(event){
	// 	var braking = 5
	// 		, x = event.clientX
	// 		, y = event.clientY
	// 		, contactsWidth = this.offsetWidth
	// 		, contactsHeight = this.offsetHeight
	// 		, xMousePositionFromCenter = Math.round(-((x/contactsWidth)*100-50)/braking)
	// 		, yMousePositionFromCenter = Math.round(-((y/contactsHeight)*100-50)/braking);

		// var coor = "Coordinates: (" + xMousePositionFromCenter + "," + yMousePositionFromCenter + ")";
		// contactsBlock.style.backgroundPosition = xMousePositionFromCenter + "% " + yMousePositionFromCenter + "%";
		// contactsParallaxBackground.style.transform = "translate(" + xMousePositionFromCenter + "%, " + yMousePositionFromCenter + "%" +")";
		// contactsParallaxBackground.style["-webkit-transform"] = "translate(-" + xMousePositionFromCenter + "%, -" + yMousePositionFromCenter + "%" +")";
		// console.log(coor);
	// };
	
// })();